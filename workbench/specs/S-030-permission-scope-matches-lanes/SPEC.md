# S-030 - Permission Scope Matches Declared Lanes

**Spec ID:** S-030
**Status:** complete
**Priority:** 1
**Owner:** claude-fable-5-1
**Stance:** Builder
**Updated:** 2026-09-05
**Catalog description:** Ship a template permission file that grants Edit and Write on the Workbench authorship lanes the prose already declares writable, and make doctor report when a room's permission file withholds a declared lane.
**Blockers:** none
**Latest event:** Spec completed and removed from the hot board.
**Next gate:** none

## Outcome

A room generated or adopted with `.claude/` copied in can record its own ADRs,
specs, wiki pages, feedback rows, and checkpoints without an agent stalling on
an ungranted write, and a permission file that disagrees with the manifest's
declared lanes is a named finding rather than a silent stall.

## Why It Matters

`templates/AGENTS.md:84` declares the `workbench/` support lanes writable in
prose. `templates/.claude/settings.json`, the mechanical half of the same
scope, never mentions `workbench/` and carries no `Write(...)` rule in its
`allow` bucket for any path at all, only `Edit(...)` (its two `Write` rules
are denies). Edit revises a file that exists; Write creates
one. Under the shipped template every new `SPEC.md`, ADR, wiki page, and
promoted checkpoint is an ungranted write while the controls say the lane is
writable. Master Workbench, generated by Genesis v3.1.1, carried exactly this
defect until 2026-09-05 (`~/Master_Workbench/data/upstream/v3.1.1.json`,
UP-012, rank `high`): it passed `doctor`, `verify`, and its 88-test suite in
that state and was found only when the owner reported that the room could not
update itself. Every other Genesis- or Adoption-generated room that copied
`.claude/` still has it.

## Current Verified State

Verified in this repository at `b7b23dd3f0929e37276880335cd4d4cc60238d8e`
on 2026-09-05:

- `templates/.claude/settings.json` `allow` holds `Edit(...)` for
  `[PRIMARY_SOURCE_DIR]`, `[TEST_DIR]`, and five root controls (`LEXICON.md`
  is not among them), plus two `Bash(...)` placeholders. It has no `Write`
  rule in `allow` (only two `Write` denies), no `workbench/` entry in any
  bucket, and no `Bash` entry for the
  Workbench authoring tools (`adr.mjs`, `sessions.mjs`, `wiki.mjs`,
  `spec-workbench.mjs`).
- `templates/.claude/README.md:12-16` maps `writable roots -> allow` and does
  not distinguish Write from Edit or name the lanes. `templates/GENESIS.md`
  Phase 4 and `templates/ADOPTION.md` Phase 4 repeat the phrase.
- The completion boxes (`templates/GENESIS.md` "`.claude/settings.json` is
  filled from that scope"; `templates/ADOPTION.md` equivalent) ask only that
  the file is filled. `workbench/tools/template-placeholders.mjs:93` verifies
  that the `[WRITABLE_ROOTS]` prose placeholder was replaced and never opens
  the permission file. No diagnostic in `workbench/tools/diagnostics.mjs`
  reads it.
- The repaired Master Workbench file (`~/Master_Workbench/.claude/settings.json`)
  is a working reference shape: Edit and Write on the five authorship lanes,
  `workbench/tools/**` in `ask`, and `Bash(node workbench/tools/*.mjs:*)`
  entries.
- This repository has no `.claude/settings.json`; its `CLAUDE.md` relies on the
  host permission layer. The dogfood boundary therefore applies to the template
  and the diagnostic, not to a root file.

Gap: everything under Desired Behavior.

## Desired Behavior

1. `templates/.claude/settings.json` grants `Edit` and `Write` on
   `workbench/specs/`, `workbench/docs/`, `workbench/wiki/`,
   `workbench/feedback/`, and `workbench/sessions/`; holds
   `workbench/tools/**` in `ask` for both Edit and Write; pairs `Write` with
   every `Edit` on the writable roots; adds `Edit(./LEXICON.md)`; and grants
   `Bash(node workbench/tools/spec-workbench.mjs:*)`, `adr.mjs`,
   `sessions.mjs`, `wiki.mjs`, and `workbench-layout.mjs`.
2. `templates/.claude/README.md` states that Edit and Write are separate
   grants, that creating a record needs Write, and lists the authorship lanes
   as a fourth row of the mapping table. `templates/GENESIS.md` and
   `templates/ADOPTION.md` Phase 4 say "writable roots and the Workbench
   authorship lanes -> allow (Edit and Write)".
3. `doctor` reports `permission-scope-drift` (severity `error`, scope
   `controls`, effect `none`) when `.claude/settings.json` exists and any
   manifest lane other than `tools` lacks a covering `Edit` and `Write` allow
   rule, is covered by a `deny` rule, or the `tools` lane appears in `allow`.
   The finding names each withheld lane. `validate --genesis` fails closed on
   it when the file exists; a room without the file is unaffected.
4. The Genesis and Adoption completion boxes read "filled from that scope and
   grants Edit and Write on the declared authorship lanes, or `.claude/` was
   omitted with a reason".

## Decisions And Contracts

- **Prose and mechanism must agree, and the mechanism is checked.** The
  permission file is the mechanical half of `AGENTS.md` Edit Scope; a lane the
  manifest declares that the file withholds is drift, reported by the same
  doctor that reports render drift.
- **`workbench/tools/` is `ask`, not absent.** Managed runtime tools change
  only through the explicit Workbench update (ADR-0031); the permission file
  says so rather than staying silent.
- **Report, do not rewrite.** The diagnostic never edits a room's permission
  file; a room may legitimately deny a lane and record why.
- **Conservative matcher.** The check recognizes `./<lane>/**` and a covering
  parent glob such as `./workbench/**`; anything else is treated as not
  granting, so a false "drift" is possible but a false "granted" is not.

## Non-Goals

- Repairing downstream rooms' permission files.
- Modeling the full Claude Code permission grammar or other agents' permission
  systems; Codex and Gemini read the prose scope.
- Adding a `.claude/settings.json` to this repository, which relies on the host
  permission layer and says so in `CLAUDE.md`.

## Dependencies And Blockers

- none

## Vertical Implementation Slices

| Ticket | Slice | Status | Blockers | Proof |
|---|---|---|---|---|
| TK-001 | Template permission file and README grant the authorship lanes with Edit and Write; protocol wording and completion boxes updated | done | none | node tools/test-workbench-layout.mjs (new test: the template permission file grants Edit and Write on every authorship lane the prose declares writable; red at ae60d8d on 'workbench/docs must have an Edit allow rule', green after the fill); node tools/test-workbench-dogfood.mjs; node tools/evaluate-workbench.mjs --path templates --include-controls (106.6/113, identical to the pre-change baseline) |
| TK-002 | `permission-scope-drift` in doctor and the Genesis gate, with the conservative glob matcher | done | TK-001 | node tools/test-diagnostics.mjs (new test: permission-scope-drift names each withheld authorship lane without blocking doctor; red at b587350 on 'Unregistered diagnostic code: permission-scope-drift'); node tools/test-workbench-layout.mjs (new test: Genesis readiness fails closed on a permission file that withholds a declared authorship lane; red at b587350 with validate returning valid); node tools/test-workbench-dogfood.mjs; node tools/evaluate-workbench.mjs --path templates --include-controls (identical to baseline); demo: doctor and validate --genesis on a disposable Genesis room with the pre-fix, fixed, and absent permission files |

### TK-001 - The template says what the prose says

**Stance:** Builder

Red first: a test that parses `templates/.claude/settings.json` and asserts an
`Edit` and a `Write` allow rule for each of the five authorship lanes, an
`ask` rule for `workbench/tools/**`, and a `Write` beside every `Edit` on the
writable roots fails today. Then fill the template, README, GENESIS, and
ADOPTION wording. `tools/test-workbench-dogfood.mjs` or the placeholder test
proves the template still carries only vocabulary placeholders.

### TK-002 - Drift is a finding

**Stance:** Builder

Red first: a disposable Genesis project whose `.claude/settings.json` is the
pre-fix template shape must yield `permission-scope-drift` naming five lanes
from `doctor` and fail `validate --genesis`; the fixed shape yields none; a
project without the file yields none. Register the code and describe it in
`tools/test-diagnostics.mjs`.

## Acceptance Criteria

- [x] The template grants Edit and Write on the five authorship lanes, holds `workbench/tools/**` in `ask`, pairs Write with Edit on writable roots, and grants the authoring-tool Bash entries.
- [x] `.claude/README.md`, GENESIS, and ADOPTION explain Edit versus Write and name the lanes; the completion boxes ask for the grant.
- [x] `doctor` reports `permission-scope-drift` naming each withheld lane; `validate --genesis` fails closed on it; rooms without the file are unaffected.
- [x] The full required suite, render, and doctor pass.

## Testing Seams

- Parsed `templates/.claude/settings.json` buckets (a JSON fixture, not a
  running Claude Code).
- `doctor(root)` and `validate --genesis` against disposable projects with the
  old, fixed, and absent permission files.
- `describe('permission-scope-drift')` in `diagnostics.mjs`.

## Verification Procedure

```bash
node tools/test-diagnostics.mjs
node tools/test-workbench-layout.mjs
node tools/test-workbench-dogfood.mjs
node tools/evaluate-workbench.mjs --path templates --include-controls
```

Then the full `AGENTS.md` verification suite, `render`, `doctor`, and
`git diff --check`.

## Documentation Impact

- `templates/.claude/README.md`, `templates/GENESIS.md`,
  `templates/ADOPTION.md`: Edit versus Write, lanes in the mapping, completion
  boxes.
- `RUNBOOK.md` (root and template): the new doctor finding and how to resolve
  it.
- `AGENTS.md`: `Docs checked; no update needed` expected; the prose scope is
  already correct.

## Append-Only Evidence And Execution Log

| Date | Ticket | Event | Verification | Docs | Remaining gap |
|---|---|---|---|---|---|
| 2026-09-05 | spec | Spec captured from upstream fix-list item UP-012 (rank high, found in Master Workbench on 2026-09-05) | Template file re-read at `b7b23dd`: no `Write` rule, no `workbench/` entry, no `LEXICON.md` edit rule; `template-placeholders.mjs:93` checks the prose placeholder only | Blueprint v3.1.2 direction links this spec | Both slices |
| 2026-09-05 | TK-001 | Ticket closed | node tools/test-workbench-layout.mjs (new test: the template permission file grants Edit and Write on every authorship lane the prose declares writable; red at ae60d8d on 'workbench/docs must have an Edit allow rule', green after the fill); node tools/test-workbench-dogfood.mjs; node tools/evaluate-workbench.mjs --path templates --include-controls (106.6/113, identical to the pre-change baseline) | templates/.claude/README.md (Edit versus Write, authorship-lanes row, workbench/tools in ask, the permission-scope-drift finding), templates/GENESIS.md and templates/ADOPTION.md Phase 4 wording and completion boxes; AGENTS.md checked, no update needed: the prose Edit Scope already declares the workbench/ support lanes writable | TK-002: permission-scope-drift is not yet a doctor finding or a Genesis gate |
| 2026-09-05 | TK-002 | Ticket closed | node tools/test-diagnostics.mjs (new test: permission-scope-drift names each withheld authorship lane without blocking doctor; red at b587350 on 'Unregistered diagnostic code: permission-scope-drift'); node tools/test-workbench-layout.mjs (new test: Genesis readiness fails closed on a permission file that withholds a declared authorship lane; red at b587350 with validate returning valid); node tools/test-workbench-dogfood.mjs; node tools/evaluate-workbench.mjs --path templates --include-controls (identical to baseline); demo: doctor and validate --genesis on a disposable Genesis room with the pre-fix, fixed, and absent permission files | RUNBOOK.md (Diagnostics And Blocking Effects paragraph and Troubleshooting row for permission-scope-drift), templates/RUNBOOK.md (Workbench Lifecycle, Diagnostics, And Decision Records paragraph); AGENTS.md checked, no update needed: the prose scope is already correct | none for this slice; rooms that copied the old template repair their own file by hand or through update-harness, as the spec's Remaining Limitations state |
| 2026-09-05 | spec | Spec completed | Acceptance gates satisfied | Documentation impact recorded above | none |

## Completion Result

Completed 2026-09-05 by claude-fable-5-1 on `claude/s030-permission-scope-lanes`
(TK-001 at `b587350`, TK-002 at `62ef21f`).

**What changed.** `templates/.claude/settings.json` now grants `Edit` and
`Write` on `workbench/specs`, `docs`, `wiki`, `feedback`, and `sessions`,
pairs `Write` with every directory `Edit` on the writable roots, adds
`Edit(./LEXICON.md)`, holds `workbench/tools/**` in `ask` for both tools, and
allows `Bash(node workbench/tools/<spec-workbench|adr|sessions|wiki|workbench-layout>.mjs:*)`.
`templates/.claude/README.md` explains that Edit and Write are separate grants,
that creating a record needs Write, and lists the authorship lanes as a fourth
mapping row; `templates/GENESIS.md` and `templates/ADOPTION.md` Phase 4 and
their completion boxes ask for the grant. `workbench/tools/workbench-layout.mjs`
gains `permissionScopeDrift(project, lanes)` and `permissionScopeMessage`,
a conservative matcher that recognises `./<path>/**` and a covering parent
glob and treats anything else (or an unreadable file) as not granting.
`doctor` (through `collectionFindings` in `spec-workbench.mjs`) reports
`permission-scope-drift` (error, controls, effect `none`) naming each withheld
lane with its reason; `validate --genesis` fails closed on the same condition;
a room without the file is unaffected. The code is registered in
`diagnostics.mjs`, and the root and template Runbooks describe the finding and
its resolution. `AGENTS.md` needed no change: the prose scope was already
correct.

**Why.** Upstream fix-list item UP-012: a Genesis v3.1.1 room could not write
its own specs, ADRs, wiki pages, feedback rows, or checkpoints while passing
every check, because the permission file never mentioned the lanes and carried
no `Write` grant, and nothing read it.

**Ambiguities resolved.** (1) "the `tools` lane appears in `allow`" is read
as effectively granted: an `allow` rule covers it and no `ask` or `deny` rule
covers it, so the accepted shape of a parent `./workbench/**` allow with
`./workbench/tools/**` in `ask` is not drift, while a bare parent glob is.
(2) An unreadable permission file grants nothing and is reported as drift
naming all five lanes. (3) `Write(./[SCHEMA_OR_MIGRATIONS_DIR]/**)` was added
beside its `Edit` in `ask` so the Edit/Write pairing holds for every
directory rule, not only `allow`. (4) The matcher lives in
`workbench-layout.mjs` rather than a new module because the relocated-CLI
test and the installer's fixed `RUNTIME_TOOLS` list pin the helper set.
(5) The Bash grants use the `node workbench/tools/<tool>.mjs:*` prefix form.

**Risks and side effects.** Rooms that copied the old template now see a
nonblocking `permission-scope-drift` from `doctor` and fail
`validate --genesis` until they add the lane rules or record a deliberate
denial; the finding tells them which lanes. The matcher is conservative by
contract, so legitimate but unrecognised shapes (`workbench/**` without `./`,
`//workbench/**`, per-file globs) report drift; the Runbooks say to widen the
rule. The template now allows the five authoring tools under Bash without a
prompt in a generated room.

**How verified.** Red/green at the named commits: the TK-001 template test
failed on `workbench/docs must have an Edit allow rule`, the TK-002 diagnostics
test on `Unregistered diagnostic code: permission-scope-drift`, and the gate
test with `validate --genesis` returning `valid` for the pre-fix file.
`node tools/test-diagnostics.mjs` (5/5), `node tools/test-workbench-layout.mjs`
(21/21), `node tools/test-workbench-dogfood.mjs`, and
`node tools/evaluate-workbench.mjs --path templates --include-controls`
(106.6/113, byte-identical to the pre-change baseline) pass; the full
`AGENTS.md` suite, `render`, `doctor`, and `git diff --check` were run after
completion (see the evidence row). Demo artifact: a scripted completed Genesis
room shows absent -> `valid`/no findings, pre-fix -> `permission-scope-drift`
naming `docs,specs,wiki,sessions,feedback` from both `validate --genesis`
(exit 1) and `doctor` (exit 0), fixed -> `valid`/no findings.

## Remaining Limitations Or Follow-Up Specs

- Rooms that already copied the old template must apply the new grants by
  hand or through update-harness; the diagnostic tells them which lanes.
- The version stamp belongs to
  [S-035](../S-035-workbench-v3-1-2-candidate/SPEC.md).

## Supersession

- Supersedes: none
- Superseded by: none
