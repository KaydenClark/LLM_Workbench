# Direct Blueprint Tasks: bounded ownership proposal for S-00O

Planning only; inspected 2026-09-26 at `89d4042fb8931b9d720af75bffea1c28803d72aa`. No allocation, runtime change, accepted ADR change, or lifecycle write is performed. All repository paths and source observations below refer to `git show 89d4042fb8931b9d720af75bffea1c28803d72aa:<path>` unless explicitly identified as the current assignment. This is a linked proposal under S-00O, not a second task-state or proof store; the assigned Spec remains the delivery/state owner.

## Recommendation

Assign a future, explicitly authorized successor to completed Task Artifact And Terminology Migration — S-00H to own the direct-Task artifact capability. Do not reopen S-00H or make release orchestrator S-00O the implementation owner. Suggested title: **Direct Blueprint Tasks And Task Artifact Ownership**; no Spec ID allocated. S-00O accounts for its reviewed delivery in release planning. Workflow Canon Rework — S-00P owns consequential control reconciliation, with one writer for shared controls. This proposed successor ownership needs Director disposition before a new capability packet is created. This packet is a proposal, not creation authority.

The capability owns a real direct Task, not a synthetic Spec. The accepted E-9 home is `workbench/tasks/TK-XXXX/TASK.md`; the board's spec field is empty, its lane advances independently, and its ID belongs to the room-wide TK series. Preserve Spec-owned and existing corrective Tasks. A directly assigned small change remains bounded by the Blueprint destination and Contract. Work expected to outgrow one context unit becomes a Spec; an interrupted run alone does not force conversion or delete attempts.

Latest assignment governs the roles: Worker self-checks and reports; Dispatcher evaluates destination QA; independent Director reviews the assembled delivery. A direct Task follows that same chain because it is itself the assembled destination delivery. Do not add independent review to every ordinary child Task. Human QA timing is owner-led and flexible; final closure waits for main verification and durable capture. This is not authorization to promote main or an assertion that runtime supports those semantics today.

## Distinct scope test

The proposed successor owns the question **can a Task exist, be discovered and be executed without a parent Spec?** Its unique scope is direct-record home, optional-parent representation, Blueprint destination, required Packet contents and execution pickup. Folder Lifecycle — S-00I owns what happens when that record moves or is reconciled/discarded; Spec QA — S-00J owns the evaluation, bound review and corrective semantics; Workflow Canon — S-00P owns coherent controls and generic documentation. Neither lifecycle nor QA alone supplies the missing executable artifact/Packet model. S-00H is retired, so changing that completed delivery record to absorb new capability would erase the distinction between delivered history and later accepted direction. A successor is justified if Director confirms this bounded artifact capability merits its own owner; this document allocates neither ID nor work.

## Confirmed lineage and observed gap

- Ledger E-3 (decision-005) explicitly permits Tasks without Specs and requires both card grains. E-9 (decision-014) names the home, empty board spec, common TK sequence, folder lifecycle and scale guard. TT-Q3 supports Blueprint-to-Tasks; TT-Q2 explains the standalone artifact. The ledger's E-9 reason labels its reasoning as inference; do not represent that rationale as another direct owner quotation.
- ADR-000H is accepted but still defines a Task as a slice of SPEC.md and requires Spec acceptance lines in the Packet (or the implemented corrective Wiki destination). Its historic acceptance is not acceptance of a new field schema. Amend its live contract in a coherent owner-authorized candidate, preserving provenance/history.
- `workbench/tools/task-record.mjs` requires a nonempty valid `Spec ID`; allowed destinations are only `spec-acceptance` and `wiki-claim`. `listTaskRecords` discovers only a supplied parent directory's `tasks/` subtree. `task-packet.mjs` resolves only those two destination types and obtains cited paths from Spec testing seams or Wiki source_paths.
- `spec-workbench.mjs` selection starts with loaded Specs, then discarded-Spec corrective Tasks. That corrective route still carries the historical Spec ID and lives under `<specs>/corrective/tasks`; it is not general direct-Blueprint support. `moveTaskRecord` immediately calls `findSpec`, then derives the Task home from the Spec. Reusing this route without changing the ownership model would mislabel direct work.
- Manifest has no tasks lane/collection. Ledger reports the missing direct home; live reader/lifecycle seams independently substantiate that gap. `spec-report.mjs` proof hashing is Spec-centered; direct delivery cannot simply omit QA/proof binding because a Spec is absent.
- TT-Q10 already settled TK as Task prefix. Width-four/case/dual-form WBID changes belong to the separately proposed identity capability; never reinterpret recovered TT-Q10 as a still-open prefix choice.

## Dependencies and ownership

| Owner | Direct-Task relationship |
|---|---|
| Ownership Map Root Control — S-00G | Owns claim routing and durable-decision escalation semantics. Direct Tasks need explicit decision/body ownership and routes to existing durable owners, not decisions hidden in Receipt rows. Preserve unresolved FND-Q24B and proposed ADR-000B/C/D acceptance gates; direct Task implementation must not implicitly accept them. |
| Spec QA Gate At Integration — S-00J | Owns QA/review/approval binding and the corrective return path. Extend the delivery subject to a direct Task with immutable content/proof coverage, unresolved decision checks and retained failed attempts. Preserve S-00U repair behavior. |
| Folder Lifecycle For Records — S-00I | Owns move, reconciliation, retirement, discard and recovery semantics for the new home; compose main verification and feature capture. Preserve S-00T incarnation/reference/recovery protections. Direct records must not disappear under Spec-only scanning. |
| Workflow Canon Rework — S-00P | Reconciles Task/Packet definitions, role assignment, procedures, generic mirrors and dependency language to actual supported behavior; no claim of universal support after a reader-only slice. |
| Proposed WBID delivery | Supplies authoritative room-wide uniqueness and width-four/dual-form behavior, including direct active/retired homes. The direct lane consumes its identity seam; no competing allocator or bulk historical rewrite. |
| Proposed JSON board delivery | Owns generated TASKBOARD.json and shared lane calculation; consumes direct records with empty spec and independent lanes. Source ownership stays in the Task; no board-authored status or duplicate card. Coordinate rather than invent a second generator. |
| Release — S-00O | Tracks this missing build obligation, demonstration and downstream release proof. Replace justified build prerequisites with reviewed delivery milestones only through an explicit dependency reconciliation; keep main/capture closure prerequisites distinct. Existing runtime blockers are not silently bypassed. |

Landmark Tracker Foundation — S-01T is not this board or direct-Task runtime owner. Coordinate its Wiki/features and identity overlaps only. E-6's WBID -> board -> controls -> release order remains intact; prepare the direct-record contract before board consumes it, serialize implementation overlap, and integrate its finished evidence into that sequence.

## First executable vertical slice, after lane release

Deliver **one direct Task cold-clone pickup and self-check path**, in a disposable fixture, from manifest-resolved home through discovery -> selection -> Packet -> claim -> proof/Receipt -> readable delivery result. This is the smallest useful execution slice; reader-only success is insufficient. It is not final closure, retirement or QA approval.

Proposed record representation: retain `Spec ID` for existing records; represent no parent explicitly for direct records and add a typed Blueprint destination reference. The exact text/null encoding and destination name are implementation proposals to review, not recovered owner decisions. Direct TASK.md must carry bounded acceptance and cited source/test references itself; its Blueprint reference explains the destination, and Contract retains authority. Do not manufacture a Spec solely to supply missing Packet members. Preserve existing parse/format round trips.

Red fixture at `tools/test-spec-workbench.mjs`, using exported runtime seams, must fail because a declared direct Task cannot be discovered/selected and its Packet rejects absent Spec ownership. Green must select its one room-unique ID, assemble executable bounded context from tracked files alone, claim it, append self-check evidence and expose its outcome without claiming QA or final closure. Include a child Task and an existing orphan corrective Task to prove no duplicate/synthetic ownership, and a blocked direct Task to prove blockers are enforced rather than inheriting the corrective path's `none` assumption. Reject wrong-home ownership, duplicate canonical IDs, invalid/missing Blueprint destination and out-of-room references. No live records are fixtures.

Later slices compose the board, J's destination QA/Director review and I's main -> capture -> retire -> discard -> recovery path. Before capability acceptance, run the complete continuous demonstration with stale-content approval refusal, unreconciled-decision refusal, retained failed attempts, link repair, whole-directory recovery and fresh-clone readability. Run targeted red/green tests plus the Contract full suite, guardrail baseline/after and pre/post self-drift. Tests establish their bounded behavior, not owner QA approval.

## Exact shared paths requiring Director lane release

Primary runtime lane: `workbench/manifest.json`, `workbench/tools/workbench-layout.mjs`, `workbench/tools/task-record.mjs`, `workbench/tools/task-packet.mjs`, `workbench/tools/spec-workbench.mjs`, `workbench/tools/task-receipt.mjs`; fixture lane: `tools/test-spec-workbench.mjs`, `tools/test-workbench-layout.mjs`. The first slice may leave receipt bytes unchanged if the existing interface suffices; trace before editing.

Cross-lane contracts: J controls `workbench/tools/spec-report.mjs` and `tools/test-spec-report.mjs`; WBID controls `workbench/tools/visible-ids.mjs` and the identity fixtures in `tools/test-workbench-identity.mjs`; board shares `workbench/tools/spec-workbench.mjs` and `TASKBOARD.md` and proposes new `TASKBOARD.json` (not yet an existing owner). Serialize these paths, do not assign simultaneous edits.

Control/docs lane: `AGENTS.md`, `LEXICON.md`, `RUNBOOK.md`, `BLUEPRINT.md`, matching `templates/AGENTS.md`, `templates/LEXICON.md`, `templates/RUNBOOK.md`, `templates/BLUEPRINT.md`, `templates/TASKBOARD.md`; ADR `workbench/docs/adr/000H-a-task-is-a-standalone-artifact-and-task-replaces-ticket-as-the-execution-slice-term.md`; durable explanation `workbench/wiki/design-concepts/task-artifact-and-lifecycle.md`. The generic runtime mirror is produced through the existing layout/managed installation route; no `templates/workbench/tools/task-record.mjs` file was found or invented. Any new directory/fixture/schema must be explicitly named in the released implementation packet.

S-00O and dependent Spec state, manifest allocation/catalog and projections retain one durable writer: Dispatcher/Director's designated writer. This Worker requests no authority over those records.

## Representation choices and source limits

The following are proposals requiring disposition within the authorized implementation packet: absent-parent encoding in TASK.md; typed Blueprint-reference syntax; location/shape of direct Task acceptance and cited paths; manifest tasks lane declaration/migration compatibility; public command addressing without a Spec argument; and bound direct-delivery review representation shared with J. E-9 settles the home and empty board spec field, not all these serialization details. Do not infer approval of FND-Q24B or ADR-000B/C/D from implementation convenience.

Source observations are anchored to the SHA above: AGENTS.md, RUNBOOK.md Ordinary Entry, LEXICON.md routing, this directory's SPEC.md, ADR-000H, `workbench/wiki/grilling-destination-audit-ledger.json` E-3/E-9/TT lineage, and the named source seams. Current assignment supplies Director > Dispatcher > Worker separation, the same chain for direct Tasks, flexible owner-led Human QA timing and main verification/capture before closure; these newer instructions are distinguished from older committed role wording. Recovery conversation and local notes informed navigation only and are not durable acceptance evidence here.

Read-only planning inspection ran doctor with no blocking findings and seven attention findings, including stale S-00Q claim; that observation belongs to the anchored inspection, not a promise of current repository health. No behavioral implementation, remote freshness verification, implementation test, full suite, allocation, approval, lifecycle mutation or release-readiness proof was performed. Planned red/green cases above are acceptance proposals, not results. Dispatcher remains sole writer of Spec state and evidence.
