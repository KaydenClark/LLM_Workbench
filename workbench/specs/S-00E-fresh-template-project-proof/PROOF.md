# Fresh Puffer Pond proof

## Boundary and sources

The owner named Puffer-Pond as the disposable test project. This run forms a new
room from a clean remote Workbench Template copy; it does not adopt the old
Puffer application or reset its main branch. The manager selected the small
demonstration scope under that assignment, rather than inventing new interview
answers from Kayden.

- Template: `KaydenClark/Example_Workbench`, integration
  `40100037562f1327f17afcba4cd6ff784321e35e`, identity
  `WB-1RX8E0KgeMDxKV6Z3l9AEC`.
- Historical product and three MIT artwork sources: `KaydenClark/Puffer-Pond`,
  main `93c3d2cf9bb4fba5f3e501f7ebc0670b9435ed11`. The project preserves the
  license and per-file provenance; no historical application source was copied.
- Generator implementation: Workbench `0174675`, before the release version
  stamp. The source and Template both declared v3.2.0 at this stage. Commit
  identity, rather than a later version label, names the observed implementation.
- Prepared local inputs: a clean task branch at `55a3fba`, selected Q1/Q2,
  their recorded manager-demonstration interpretations and active ADR-0002.
  The public generator copied the relevant bytes into the fresh room. It does
  not claim that the local preparation commit exists on the Template remote.
- Fresh room identity: `WB-0eSjLULGezLPlWk6RzkPpe`, distinct from the Template.
  Its [derivation receipt](proof/derivation.json) binds the plan, note, evidence,
  Template controls and selected decisions to their hashes.

The bounded outcome is a full-screen illustrated pond with five puffers plus
snails, local static source and no package dependencies. Desktop and phone
layout, reduced motion and project-local checks are acceptance requirements.

## Native execution

Host: macOS, Codex CLI 0.144.3, Node v26.3.0. Each native session starts with
project-only instructions and `--ignore-user-config --ephemeral`; neither is
instructed to load the developer checkout or personal skill catalog. The browser
verification package is a host tool, not a dependency of the resulting site.
This is one-host, fresh-context continuation evidence. It does not establish
other-provider behavior, cross-device recovery, automatic skill discovery or
repeated reliability.

The builder session was configured as `gpt-5.3-codex-spark`, High. It produced
`a368a6a1d28f47654326a5406372c8a64de1c746`, six passing local tests, a successful
static build and doctor, and a saved JSON work note at revision 4. Browser
acceptance remained explicitly pending. Selected actual command/result pairs
are preserved in [builder checks](proof/builder-checks.json).

The builder's initial nonexistent-test failure is not behavior-level red proof.
Its first implementation had three puffers and two snails, and its ticket was
still ready. A spurious partial commit string appeared in one note entry;
a later entry contained the actual full SHA. These are observed execution gaps,
not accepted correctness claims. The continuation must reconcile the note,
claim the existing ticket and prove the missing acceptance before completion.

A separate session configured as `gpt-5.5`, High, read note revision 4 and
recovered the committed-source/pending-browser state. It reproduced the species
count failure (`3 !== 5`), corrected five puffers plus two snails, and added a
favicon after a real browser 404. Seven Node tests, build, doctor and the two
browser viewports passed at `ce6a43c54c9f7bef0a54efb55b24eab6dbd735fd`.
See [continuation checks](proof/continuation-checks.json) and the
[native browser result](proof/native-browser.json). The saved note reached
revision 10 before the manager resumed.

Manager production verification then reproduced a separate credit-link 404
against the built `dist` tree. The page linked to a Workbench document absent
from that output. The smallest correction links the bundled MIT license.
[Production check](proof/production-check.cjs), committed at Workbench939c551,
failed before the correction and passed afterward at project
`75a242dd2d8e5a03295e37f3858e32b37868ede5`: license200, five puffers/two snails,
loaded images, no overflow/browser errors, underwater bounds and reduced-motion
pause at both sizes. Node7/7, build and doctor also passed. Remaining draft
wording was filled and `dist` excluded from Git. The manager linked the spurious
note entry to an explicit correction; the current note is revision12.

The [production result](proof/production-result.json),
[desktop screenshot](proof/production-desktop.png) and
[phone screenshot](proof/production-phone.png) are the final visual proof.
Reproduction uses the project build served on localhost4174 and the check above,
with `PLAYWRIGHT_MODULE` and `CHROME_EXECUTABLE` identifying the host browser tools.

A clean Git clone of project75a242d retained its identity, while the Genesis
copy's identity differs from the Template. [Identity check](proof/identity-check.json)
records that distinction; cloning an existing room does not create a new one.

## Delivery state

The useful project and saved-note continuation are verified locally. Recovery
publication is scoped to a new Puffer-Pond task branch, preserving its original
main. Final Workbench version verification and independent integration review
remain release gates. No deployment or repeated-reliability claim is made.

## Release-stamp check

After native acceptance passed, the Workbench manifest was marked v3.2.1.
The public generator at22f8705 then consumed the same clean Template4010003
and explicitly restamped draft inputs23231c9. It produced a second, empty
first-capability room with identity `WB-3dYnaJXFRmzhUzho6iBsmA`, v3.2.1
runtime provenance and valid Genesis readiness. This is a structural release
check, separate from the native useful-project observation above.

Doctor also reports the host's installed v3.2.0 core skills as incompatible
with v3.2.1 (42 attention findings across two discovery paths), plus the draft
README's room-brain link attention. These do not block room creation. No
personal installation was updated or represented as a v3.2.1 deployment.
The isolated installer tests remain the proof for new-install behavior.
